terraform {
  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 4.0"
    }
  }

  backend "gcs" {
    bucket  = YOUR_BUTCKET_HERE
    prefix  = "terraform/state"
  }
}
